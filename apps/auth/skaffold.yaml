apiVersion: skaffold/v2beta17
kind: Config
metadata:
  name: auth-api
build:
  local:
    push: false
  artifacts:
  - image: diskit/auth-api
    context: docker
    docker:
      dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
    - k8s/deployment.yaml
    - k8s/service.yaml
